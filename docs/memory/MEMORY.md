# Memory index — v-rpc-tap (per-repo)

Per-repo durable memory for the `v-rpc-tap` repo (the hybrid Go+M `v` domain that
ships the **VSL RPC TAP** M package — the in-path RPC-broker tap — plus its Go
host). The **shared** cross-cutting tap proposal/tracker/coordination memory lives
in the central `docs` repo (`proposals/v-rpc-tap-scalable*`, `memory/v-rpc-tap-scalable.md`);
this file holds only facts durable to THIS repo's code. One line per entry — detail
in the linked topic file. Keep-test applies (durable lessons only, not per-increment status).

- [P2 L-block safety harness](p2-lblock-safety-harness.md) — in-path fences (fail-open trap, naked-ref fence, zero-I/O) proven under fault by `tests/VSLRTLTST.m` (L1–L3) + ring multi-actor invariants (single trim owner D8, segmentation D13, durability watermark D12) proven by `tests/VSLRTCTST.m` (L4–L6). Class-agnostic trap; the **deterministic-interleaving** proof technique (sound: no shared written node + atomic `$INCREMENT`); the **inc-token same-second/same-DUZ collision** gotcha + reaper-is-load-bearing-for-segmentation; the **STDASSERT `$X`/`$Y`** + simulate-an-append-fully test gotchas. Plus **L9/L10 method-C ring stress** (`VSLRTCTST`'s sibling `VSLRTLDTST`): exact drop accounting (no silent loss) + the **stuck-host insight** (drain-without-committrim → watermark forbids loss → ring grows unbounded → reaper `overflow()` is the *only* backstop; watermark+overflow are a pair). 5000-load wall-clock knee = deferred L7.
- [P3 host + L14 correlation](p3-host-l14.md) — the Go host's `internal/tap` (drain parse + L14 per-[XWB]-message contract). Key insight: **one ring record IS one message** (req@15.5 + rsp@17.5 augment the SAME seq), so L14 is **classification** (complete/unpaired/names-only + empty-name) + `(inc,job,seq)` keying + `(inc,job)` session segmentation + re-drain dedup (prefer the copy with a result) — **NOT** req↔resp pairing (corrects the proposal's "correlate by call_id"). Also: the CLI `Control` verbs (arm/disarm/status/drain/committrim, `rpctapcli`) over the mdriver-backed Execer seam (`VRPCTAP_*` env; shared clikit, not the driver fork), and the **drain wire-format hardening DONE** — length-prefixed binary-safe value line + `drop` in the J header → `Record.Drop`/`Summary.Dropped` (R20). Live smoke deferred on the v-pkg install path.
- [P3 drain → S3 pipeline](p3-drain-pipeline.md) — the read-only **drain → ship → committrim-after-ack** flow (`internal/pipeline`, pure Go, fake-tested). **At-least-once D12**: commit the engine trim ONLY after `Sink.Put` acks — a failed put trims nothing (re-drain). committrim per distinct job to drained `SeqMax`. **GovCloud egress pin (U4) enforced in `Config.Validate`** (partition `aws-us-gov` + `us-gov-*` region), not at an AWS layer. `MemSink`/`FileSink` carry the flow now; **real S3 sink + CLI `ship` verb DEFERRED**. See [[p3-host-l14]].
- [P3 load-harness rig (L8)](p3-load-harness.md) — the synthetic-`[XWB]` load rig: `internal/xwbwire` (wire encoder, seeded from v-rpc-debug) + `internal/load` (`Run` drives N concurrent workers in Total/Duration mode → throughput + nearest-rank p50/p95/p99; `Delta(control,armed)` = the latency tax) + the `load` verb. **It's a broker TCP CLIENT (not the engine seam)** — takes `--addr`, no `mdriver`, the same legit exception as v-rpc-debug `ping`; waterline-clean. Rig + control run independent NOW; the **armed measurement + 5000-session L7 knee wait on the splice**. Fake-broker tested. See [[p3-host-l14]].
- [P4 splice build](p4-splice-build.md) — P4 step 1 DONE: `internal/splice` + `rpc-tap splice` verb + `cmd/rpctap` build the foreign-overwrite KIDS build (splice national XWBPRS at CALLP+9.5/+11.5 · ship VSLRT* · #19 OPTION `run^VSLRTRP`). Regenerated per-site from the live XWBPRS (`make kids SPLICE_SRC=…`; pull via `m sync diff`), never frozen. **Full `v pkg install→verify→uninstall` byte-clean on vehu** (partition restore, foreignRestore exact); all safety guards held. Stress-test FOUND+FIXED 2 v-pkg bugs (pre-image clobber + lowercase-tag validator, v-pkg@aeadb7e). OPEN: L12 patch-pin + L15 drift gates not yet built. See [[reaper-live-proof]].
- [P4 handshake client](p4-handshake-client.md) — durable invariant: driving real RPCs through the splice REQUIRES a `TCPConnect` handshake first; a bare RPC chunk is rejected at `NEW^XWBTCPM:137` before `MAIN`/`CALLP`, so the tap never fires (the old `xwbwire.RPCMessage` + L8 `load.fire()` were handshake-less → **latent L8 defect**: the armed run captured nothing). Built the handshake client TDD (2026-06-30): `internal/xwbwire` (`Connect`/`RPCMessage`/`Bye`, bytes VERBATIM from `XWBTCPMT.m`; the `54f` empty param chunk is required or `CAPI` UNDEFs) + `internal/broker` `Session` (Dial+handshake, `Fire`/`FireRaw`, read-until-`0x04` EOT) + `rpctapcli` `drive` verb (one session, fires `--rpc` in order, reports exact response bytes for the L1-L3 non-interference diff). **L1-L3 PROVEN LIVE on vehu (YDB) 2026-06-30**, gold master left byte-clean: capture + byte-exact non-interference (CF4) are real-socket; the fault path (L1-fault/L2/L3) is in-engine via `m vista exec` on the REAL installed routine (you CANNOT force a tap-fault through the wire — the broker walks the params first). **Mirrored on foia (IRIS) → both-engine gate MET, both gold masters byte-clean.** GOTCHA: `m-iris sync diff` is a FALSE NEGATIVE for the live splice (reads a mirror) — verify IRIS routine state via `m vista exec $T(...)`. See [[p4-splice-build]], [[p3-load-harness]], [[p2-lblock-safety-harness]].
- [Reaper live proof](reaper-live-proof.md) — VSLRTRP's live-only behaviours proven on BOTH gold masters (vehu YDB + foia IRIS) via inline `m vista exec`: the **L13 liveness token** (YDB `$ZGETJPI"ISPROCALIVE"` / IRIS `^$JOB`, runtime-detect XECUTE picks the branch live) + **`REQ^%ZTLOAD` requeue** queues a real task with the reaper's `ZT*` shape. Gotchas: **`DEQ^%ZTLOAD` doesn't exist → use `KILL^%ZTLOAD`** (tasks persist in `^%ZTSK` across exec procs — always clean up); **`m vista exec` swallows FOR-loop output**; **set `M_IRIS_BIN`** (m-iris not auto-discovered). **ZVPKGRD CLOSED 2026-06-30: an m-ydb env gap (source-store read routine dirs from HOST $ydb_routines, never the container) — fixed in m-ydb (`ContainerRoutines` auto-discovery); full `v pkg install→verify→uninstall` now byte-clean on vehu (status 3). P4 install precondition HEALTHY.**
